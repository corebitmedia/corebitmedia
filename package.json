{
  "name": "corebitmedia",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "node scripts/setup.js",
    "dev": "concurrently -n BACKEND,ADMIN,FRONTEND -c blue,magenta,green \"npm run dev --prefix backend\" \"npm run dev --prefix admin\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
